module: main
description: >-
  CLI entry point that parses commands and dispatches to check, init, refine,
  diff, toposort, deps, and init-skill workflows.
language: rust
source_path: src/main.rs
source_hash: 0e5a50b3749d3ab4
layer: presentation
exposes:
  Commands:
    kind: variable
    description: >-
      CLI subcommands: check, init, refine, diff, toposort, deps, init-skill.
  Cli:
    kind: variable
    description: Top-level clap CLI parser.
  ProjectConfig:
    kind: variable
    description: Deserialized project configuration from .spec-checker.yaml.
  DependencyEntry:
    kind: variable
    description: A single dependency declaration in project config.
  LlmFileConfig:
    kind: variable
    description: LLM settings section of the project config file.
  ProviderFileConfig:
    kind: variable
    description: Named LLM provider entry in the project config file.
forbidden_deps:
  - smt
  - type_formula
  - types
  - extractors
owns_state:
  - VERSION
  - Cli
  - Commands
  - ProjectConfig
  - DependencyEntry
  - LlmFileConfig
  - ProviderFileConfig
